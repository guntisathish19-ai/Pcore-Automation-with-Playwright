{
  "name": "pcore-automation-with-playwright",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npm run clean && playwright test",
    "test:report": "npm run clean && npx playwright test LoginTest.spec.js && npx allure generate allure-results --clean -o allure-report",
    "clean": "rimraf allure-results allure-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.3.1",
    "allure-commandline": "^2.40.0",
    "allure-playwright": "^3.8.0",
    "dotenv": "^17.4.2",
    "rimraf": "^5.0.10"
  },
  "dependencies": {
    "exceljs": "^4.4.0"
  }
}